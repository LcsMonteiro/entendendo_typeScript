namespace Geometria {
    export namespace Area {
       const Pi =3.14

       export function retangulo(base:number, altura: number): number {
           return base * altura
           
       }
   }
}